<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <base href="<?php echo BASE_URL; ?>" />
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=DM+Serif+Display:ital@0;1&family=Quicksand:wght@300..700&family=Roboto:ital,wght@0,100..900;1,100..900&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="<?= BASE_URL ?>css/style.css">
    <title>PolePosition</title>
</head>

<body>
    <header>
        <nav class="navbar navbar-expand-lg bg-white shadow-sm py-2 position-fixed top-0 z-1 w-100">
            <div class="container-fluid container-lg">
                <a class="navbar-brand fw-bold fs-4 text-primary" href="<?= BASE_URL ?>">PolePosition</a>

                <div class="d-flex align-items-center order-lg-3">
                    <?php if (isset($_SESSION['usuario'])): ?>
                        <a class="nav-link d-none d-lg-block text-secondary me-3" href="<?= BASE_URL ?>admin">
                            <i class="bi bi-gear-fill me-1"></i> Panel Admin
                        </a>

                        <form method="post" action="authController.php?action=logout" class="d-inline ms-2">
                            <button type="submit" class="btn btn-outline-danger btn-sm rounded-pill px-3">
                                <i class="bi bi-box-arrow-right"></i> Salir
                            </button>
                        </form>

                    <?php else: ?>
                        <a href="<?= BASE_URL ?>login" class="btn btn-custom rounded-pill px-4 ms-2">Login</a>
                    <?php endif; ?>
                </div>

                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>

                <div class="collapse navbar-collapse order-lg-2" id="navbarNav">
                    <ul class="navbar-nav ms-auto">
                        <li class="nav-item">
                            <a class="nav-link fw-semibold mx-2" href="<?= BASE_URL ?>">Inicio</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link fw-semibold mx-2" href="<?= BASE_URL ?>carreras">Carreras</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link fw-semibold mx-2" href="<?= BASE_URL ?>pilotos">Pilotos</a>
                        </li>

                        <?php if (isset($_SESSION['usuario'])): ?>
                            <li class="nav-item d-lg-none">
                                <a class="nav-link text-secondary" href="<?= BASE_URL ?>admin">
                                    <i class="bi bi-gear-fill me-1"></i> Panel Admin
                                </a>
                            </li>
                            <li class="nav-item d-lg-none">
                                <span class="navbar-text ms-3 fw-semibold">Usuario: <?= htmlspecialchars($_SESSION['usuario']); ?></span>
                            </li>
                        <?php endif; ?>
                    </ul>
                </div>

            </div>
        </nav>
    </header>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>