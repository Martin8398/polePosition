<?php require 'templates/layout/header.phtml' ?>

<div class="d-flex flex-column min-vh-100">
    <main class="flex-grow-1">
        <div class="container py-5 mt-5">
            <h1 class="mb-4">Editar Pilotos</h1>

            <?php if (!empty($pilotos)): ?>
                <table class="table table-striped table-bordered">
                    <thead class="table-dark">
                        <tr>
                            <th>Nombre</th>
                            <th>Apellido</th>
                            <th></th>
                        </tr>
                    </thead>
                    <tbody>
                        <?php foreach ($pilotos as $p): ?>
                            <tr>
                                <td><?= htmlspecialchars($p->nombre) ?></td>
                                <td><?= htmlspecialchars($p->apellido) ?></td>
                                <td>
                                    <button type="button" class="btn btn-warning" data-bs-toggle="modal" data-bs-target="#editarPilotoModal<?= $p->piloto_id ?>">
                                        Editar
                                    </button>
                                    <button type="button" class="btn btn-danger" data-bs-toggle="modal" data-bs-target="#eliminarPilotoModal<?= $p->piloto_id ?>">
                                        Eliminar
                                    </button>
                                </td>
                            </tr>

                            <!-- Modal editar -->
                            <div class="modal fade" id="editarPilotoModal<?= $p->piloto_id ?>" tabindex="-1" aria-labelledby="editarPilotoModalLabel<?= $p->piloto_id ?>" aria-hidden="true">
                                <div class="modal-dialog">
                                    <div class="modal-content">
                                        <div class="modal-header bg-warning text-white">
                                            <h5 class="modal-title" id="editarPilotoModalLabel<?= $p->piloto_id ?>">Editar Piloto</h5>
                                            <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Cerrar"></button>
                                        </div>
                                        <div class="modal-body">
                                            <form action="<?= BASE_URL ?>updatePiloto" method="POST">
                                                <input type="hidden" name="piloto_id" value="<?= $p->piloto_id ?>">

                                                <div class="mb-3">
                                                    <label for="nombre<?= $p->piloto_id ?>" class="form-label">Nombre</label>
                                                    <input type="text" class="form-control" name="nombre" id="nombre<?= $p->piloto_id ?>" value="<?= htmlspecialchars($p->nombre) ?>" required>
                                                </div>

                                                <div class="mb-3">
                                                    <label for="apellido<?= $p->piloto_id ?>" class="form-label">Apellido</label>
                                                    <input type="text" class="form-control" name="apellido" id="apellido<?= $p->piloto_id ?>" value="<?= htmlspecialchars($p->apellido) ?>" required>
                                                </div>

                                                <div class="text-end">
                                                    <button type="button" class="btn btn-danger" data-bs-dismiss="modal">Cancelar</button>
                                                    <button type="submit" class="btn btn-custom">Guardar Cambios</button>
                                                </div>
                                            </form>
                                        </div>
                                    </div>
                                </div>
                            </div>

                            <!-- Modal eliminar -->
                            <div class="modal fade" id="eliminarPilotoModal<?= $p->piloto_id ?>" tabindex="-1" aria-labelledby="eliminarPilotoModalLabel<?= $p->piloto_id ?>" aria-hidden="true">
                                <div class="modal-dialog">
                                    <div class="modal-content">
                                        <div class="modal-header bg-danger text-white">
                                            <h5 class="modal-title" id="eliminarPilotoModalLabel<?= $p->piloto_id ?>">Confirmar Eliminación</h5>
                                            <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Cerrar"></button>
                                        </div>
                                        <div class="modal-body">
                                            <p>¿Estás seguro que querés eliminar a <?= htmlspecialchars($p->nombre . ' ' . $p->apellido) ?>?</p>
                                            <form action="<?= BASE_URL ?>deletePiloto" method="POST">
                                                <input type="hidden" name="piloto_id" value="<?= $p->piloto_id ?>">
                                                <div class="text-end">
                                                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancelar</button>
                                                    <button type="submit" class="btn btn-danger">Eliminar</button>
                                                </div>
                                            </form>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        <?php endforeach; ?>

                    </tbody>
                </table>
            <?php else: ?>
                <div class="alert alert-info">No hay pilotos cargados.</div>
            <?php endif; ?>
        </div>
    </main>

    <?php require 'templates/layout/footer.phtml' ?>
</div>