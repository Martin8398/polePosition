<?php require 'templates/layout/header.phtml' ?>

<div class="d-flex flex-column min-vh-100">
    <main class="flex-grow-1">
        <div class="container py-5 mt-5">
            <h1 class="mb-4">Editar Pilotos</h1>

            <?php if (!empty($pilotos)): ?>
                <table class="table table-striped table-bordered">
                    <thead class="table-dark">
                        <tr>
                            <th>Nombre</th>
                            <th>Apellido</th>
                            <th></th>
                        </tr>
                    </thead>
                    <tbody>
                        <?php foreach ($pilotos as $p): ?>
                            <tr>
                                <td><?= htmlspecialchars($p->nombre) ?></td>
                                <td><?= htmlspecialchars($p->apellido) ?></td>
                                <td>
                                    <a class="btn btn-custom" href="<?= BASE_URL ?>editarPiloto/<?= $p->piloto_id ?>">Editar</a>
                                    <a class="btn btn-custom" href="<?= BASE_URL ?>eliminarPiloto/<?= $p->piloto_id ?>">Eliminar</a>
                                </td>
                            </tr>
                        <?php endforeach; ?>
                    </tbody>
                </table>
            <?php else: ?>
                <div class="alert alert-info">No hay carreras cargadas.</div>
            <?php endif; ?>
        </div>
    </main>

    <?php require 'templates/layout/footer.phtml' ?>
</div>