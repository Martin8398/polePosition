<?php require 'templates/layout/header.phtml'; ?>

<div class="d-flex flex-column min-vh-100">

    <main class="flex-grow-1">
        <?php if ($carrera): ?>
            <div class="container py-5 mt-5">
                <h1 class="mb-4">Detalles de la Carrera</h1>

                <div class="row mb-4">
                    <div class="col-md-4">
                        <div class="card text-white bg-custom mb-3">
                            <div class="card-body">
                                <h5 class="card-title">Fecha</h5>
                                <p class="card-text"><?= htmlspecialchars($carrera->fecha) ?></p>
                            </div>
                        </div>
                    </div>
                    <div class="col-md-4">
                        <div class="card text-white bg-custom mb-3">
                            <div class="card-body">
                                <h5 class="card-title">Vueltas</h5>
                                <p class="card-text"><?= htmlspecialchars($carrera->vueltas) ?></p>
                            </div>
                        </div>
                    </div>
                    <div class="col-md-4">
                        <div class="card text-white bg-custom mb-3">
                            <div class="card-body">
                                <h5 class="card-title">Participantes</h5>
                                <p class="card-text"><?= count($resultados) ?></p>
                            </div>
                        </div>
                    </div>
                </div>
                <?php if (isset($_SESSION['usuario'])): ?>
                    <div class="text-end mb-3">
                        <button type="button" class="btn btn-custom" data-bs-toggle="modal" data-bs-target="#agregarResultadoModal">
                            Agregar Resultado
                        </button>
                        <button type="button" class="btn btn-warning" data-bs-toggle="modal" data-bs-target="#editarCarreraModal">
                            Editar Carrera
                        </button>
                        <button type="button" class="btn btn-danger" data-bs-toggle="modal" data-bs-target="#eliminarCarreraModal">
                            Eliminar Carrera
                        </button>
                    </div>

                    <!-- Modal -->
                    <div class="modal fade" id="agregarResultadoModal" tabindex="-1" aria-labelledby="agregarResultadoModalLabel" aria-hidden="true">
                        <div class="modal-dialog">
                            <div class="modal-content">
                                <div class="modal-header bg-custom text-white">
                                    <h5 class="modal-title" id="agregarResultadoModalLabel">Agregar Resultado</h5>
                                    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Cerrar"></button>
                                </div>
                                <div class="modal-body">
                                    <form action="<?= BASE_URL ?>addResultado" method="POST">
                                        <input type="hidden" name="carrera_id" value="<?= $carrera->carrera_id ?>">

                                        <div class="mb-3">
                                            <label for="piloto_id" class="form-label">Piloto</label>
                                            <select class="form-select" name="piloto_id" id="piloto_id" required>
                                                <option value="" selected disabled>Seleccionar piloto</option>
                                                <?php foreach ($pilotos as $p): ?>
                                                    <option value="<?= $p->piloto_id ?>">
                                                        <?= htmlspecialchars($p->nombre . ' ' . $p->apellido) ?>
                                                    </option>
                                                <?php endforeach; ?>
                                            </select>
                                        </div>

                                        <div class="mb-3">
                                            <label for="posicion" class="form-label">Posición</label>
                                            <input type="number" class="form-control" name="posicion" id="posicion" min="1" required>
                                        </div>

                                        <div class="mb-3">
                                            <label for="tiempo" class="form-label">Tiempo</label>
                                            <input type="text" class="form-control" name="tiempo" id="tiempo" placeholder="Ej: 1:32.500" required>
                                        </div>

                                        <div class="text-end">
                                            <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancelar</button>
                                            <button type="submit" class="btn btn-custom">Guardar Resultado</button>
                                        </div>
                                    </form>
                                </div>
                            </div>
                        </div>
                    </div>
                    <div class="modal fade" id="editarCarreraModal" tabindex="-1" aria-labelledby="editarCarreraModalLabel" aria-hidden="true">
                        <div class="modal-dialog">
                            <div class="modal-content">
                                <div class="modal-header bg-warning text-white">
                                    <h5 class="modal-title" id="editarCarreraModalLabel">Editar Carrera</h5>
                                    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Cerrar"></button>
                                </div>
                                <div class="modal-body">
                                    <form action="<?= BASE_URL ?>updateCarrera" method="POST">
                                        <input type="hidden" name="carrera_id" value="<?= $carrera->carrera_id ?>">

                                        <div class="mb-3">
                                            <label for="fecha" class="form-label">Fecha</label>
                                            <input type="date" class="form-control" name="fecha" id="fecha" value="<?= htmlspecialchars($carrera->fecha) ?>" required>
                                        </div>

                                        <div class="mb-3">
                                            <label for="vueltas" class="form-label">Vueltas</label>
                                            <input type="number" class="form-control" name="vueltas" id="vueltas" value="<?= htmlspecialchars($carrera->vueltas) ?>" min="1" required>
                                        </div>

                                        <div class="text-end">
                                            <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancelar</button>
                                            <button type="submit" class="btn btn-warning">Guardar Cambios</button>
                                        </div>
                                    </form>
                                </div>
                            </div>
                        </div>
                    </div>
                    <div class="modal fade" id="eliminarCarreraModal" tabindex="-1" aria-labelledby="eliminarCarreraModalLabel" aria-hidden="true">
                        <div class="modal-dialog">
                            <div class="modal-content">
                                <div class="modal-header bg-danger text-white">
                                    <h5 class="modal-title" id="eliminarCarreraModalLabel">Confirmar Eliminación</h5>
                                    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Cerrar"></button>
                                </div>
                                <div class="modal-body">
                                    <p>¿Estás seguro que querés eliminar esta carrera?</p>
                                    <form action="<?= BASE_URL ?>deleteCarrera" method="POST">
                                        <input type="hidden" name="carrera_id" value="<?= $carrera->carrera_id ?>">
                                        <div class="text-end">
                                            <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancelar</button>
                                            <button type="submit" class="btn btn-danger">Eliminar</button>
                                        </div>
                                    </form>
                                </div>
                            </div>
                        </div>
                    </div>
                <?php endif; ?>

                <?php if (!empty($resultados)): ?>
                    <h2 class="mb-3">Resultados</h2>
                    <table class="table table-striped table-bordered">
                        <thead class="table-dark">
                            <tr>
                                <th>Posición</th>
                                <th>Piloto</th>
                                <th>Tiempo</th>
                                <th></th>
                            </tr>
                        </thead>
                        <tbody>
                            <?php foreach ($resultados as $r): ?>
                                <?php
                                $rowClass = '';
                                if ($r->posicion == 1) $rowClass = 'table-warning bg-custom2';
                                elseif ($r->posicion == 2) $rowClass = 'table-secondary bg-gradient';
                                elseif ($r->posicion == 3) $rowClass = 'table-danger bg-gradient';
                                ?>
                                <tr class="<?= $rowClass ?>">
                                    <td><?= htmlspecialchars($r->posicion) ?></td>
                                    <td><?= htmlspecialchars($r->nombre . ' ' . $r->apellido) ?></td>
                                    <td><?= htmlspecialchars($r->tiempo) ?></td>
                                    <td>
                                        <a class="btn btn-custom" href="<?= BASE_URL ?>piloto/<?= $r->piloto_id ?>">Ver piloto</a>
                                        <?php if (isset($_SESSION['usuario'])): ?>
                                            <button type="button" class="btn btn-warning" data-bs-toggle="modal" data-bs-target="#editarResultadoModal<?= $r->resultado_id ?>">
                                                Editar Resultado
                                            </button>
                                            <button type="button" class="btn btn-danger" data-bs-toggle="modal" data-bs-target="#eliminarResultadoModal">
                                                Eliminar Resultado
                                            </button>
                                            <div class="modal fade" id="editarResultadoModal<?= $r->resultado_id ?>" tabindex="-1" aria-labelledby="editarResultadoModalLabel<?= $r->resultado_id ?>" aria-hidden="true">
                                                <div class="modal-dialog">
                                                    <div class="modal-content">
                                                        <div class="modal-header bg-warning text-white">
                                                            <h5 class="modal-title" id="editarResultadoModalLabel<?= $r->resultado_id ?>">Editar Resultado</h5>
                                                            <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Cerrar"></button>
                                                        </div>
                                                        <div class="modal-body">
                                                            <form action="<?= BASE_URL ?>updateResultado" method="POST">
                                                                <input type="hidden" name="resultado_id" value="<?= $r->resultado_id ?>">
                                                                <input type="hidden" name="carrera_id" value="<?= $carrera->carrera_id ?>">

                                                                <div class="mb-3">
                                                                    <label for="posicion" class="form-label">Posición</label>
                                                                    <input type="number" class="form-control" name="posicion" id="posicion" value="<?= htmlspecialchars($r->posicion) ?>" min="1" required>
                                                                </div>

                                                                <div class="mb-3">
                                                                    <label for="tiempo" class="form-label">Tiempo</label>
                                                                    <input type="text" class="form-control" name="tiempo" id="tiempo" value="<?= htmlspecialchars($r->tiempo) ?>" placeholder="Ej: 1:32.500" required>
                                                                </div>

                                                                <div class="mb-3">
                                                                    <label for="piloto_id" class="form-label">Piloto</label>
                                                                    <select class="form-select" name="piloto_id" id="piloto_id" required>
                                                                        <?php foreach ($pilotos as $p): ?>
                                                                            <option value="<?= $p->piloto_id ?>" <?= $p->piloto_id == $r->piloto_id ? 'selected' : '' ?>>
                                                                                <?= htmlspecialchars($p->nombre . ' ' . $p->apellido) ?>
                                                                            </option>
                                                                        <?php endforeach; ?>
                                                                    </select>
                                                                </div>

                                                                <div class="text-end">
                                                                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancelar</button>
                                                                    <button type="submit" class="btn btn-warning">Guardar Cambios</button>
                                                                </div>
                                                            </form>
                                                        </div>
                                                    </div>
                                                </div>
                                            </div>
                                            <div class="modal fade" id="eliminarResultadoModal" tabindex="-1" aria-labelledby="eliminarResultadoModalLabel" aria-hidden="true">
                                                <div class="modal-dialog">
                                                    <div class="modal-content">
                                                        <div class="modal-header bg-danger text-white">
                                                            <h5 class="modal-title" id="eliminarCarreraModalLabel">Confirmar Eliminación</h5>
                                                            <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Cerrar"></button>
                                                        </div>
                                                        <div class="modal-body">
                                                            <p>¿Estás seguro que querés eliminar este resultado?</p>
                                                            <form action="<?= BASE_URL ?>deleteResultado" method="POST">
                                                                <!-- //lo necesitamos para la redirección -->
                                                                <input type="hidden" name="carrera_id" value="<?= $carrera->carrera_id ?>">

                                                                <input type="hidden" name="resultado_id" value="<?= $r->resultado_id ?>">
                                                                <div class="text-end">
                                                                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancelar</button>
                                                                    <button type="submit" class="btn btn-danger">Eliminar</button>
                                                                </div>
                                                            </form>
                                                        </div>
                                                    </div>
                                                </div>
                                            </div>
                                        <?php endif; ?>
                                    </td>
                                </tr>
                            <?php endforeach; ?>
                        </tbody>
                    </table>

                    <div class="alert bg-custom2 mt-3">
                        <strong>Ganador:</strong> <?= htmlspecialchars($resultados[0]->nombre . ' ' . $resultados[0]->apellido) ?>
                        con tiempo <?= htmlspecialchars($resultados[0]->tiempo) ?>
                    </div>
                <?php else: ?>
                    <div class="alert alert-info">No hay resultados cargados para esta carrera.</div>
                <?php endif; ?>
            </div>

        <?php else: ?>
            <div class="container my-5">
                <div class="alert alert-danger">No se encontraron detalles de la carrera.</div>
            </div>
        <?php endif; ?>
    </main>

    <!-- Footer fijo al fondo -->
    <?php require 'templates/layout/footer.phtml'; ?>
</div>